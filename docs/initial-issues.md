# Initial Issue Drafts

## v0.1.x

1. Improve validation errors with stable error categories.
   Acceptance: each error includes a category suitable for CLI automation.
2. Add JSON Schema conformance fixtures.
   Acceptance: fixtures cover required fields, patterns, enums, bounds, and formats.
3. Add a CI artifact example.
   Acceptance: documentation shows validation of a snapshot artifact in CI.
4. Review extension-field policy.
   Acceptance: document whether future optional metadata belongs in a namespaced field.
5. Add Windows path fixtures.
   Acceptance: policy tests cover representative absolute Windows paths.

## Future Candidates

1. Explore a Markdown handoff renderer.
2. Explore adapter examples based on real provider usage.
3. Explore optional protocol mappings without expanding the core contract.

